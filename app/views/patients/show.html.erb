<% @patient.appointments.each do |appt| %>
<li> Date: <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %> Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> </li>
<% end %>