<h1>Your Appointments</h1>

<% @patient.appointments.each do |p| %>
    <%= p.appointment_datetime.strftime("%B %d, %Y at %H:%M") %><br>
    <%= link_to p.doctor.name, doctor_path(p.doctor) %>
<% end %>