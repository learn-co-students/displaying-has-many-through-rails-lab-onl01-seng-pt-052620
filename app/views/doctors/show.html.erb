<h1><%= @doctor.name %> - <%= @doctor.department %>
<br>

<p>
    <% @doctor.appointments.each do |appt| %>
        <%= link_to appt.patient.name, patient_path(appt.patient) %> - <%= appt.appointment_datetime.strftime('%B %d, %Y')%> at <%=appt.appointment_datetime.strftime('%H:%M')%>
    <% end %>
</p>
